# Google GenAI API Key (可选)
# 从 https://aistudio.google.com/app/apikey 获取
# 可以提供单个密钥，或用逗号分隔的多个密钥以实现轮询
GOOGLE_GENAI_API_KEY=your_api_key_1,your_api_key_2

# OpenAI API Key (必需)
# 从 OpenAI 或兼容的提供商获取
# 可以提供单个密钥，或用逗号分隔的多个密钥以实现轮询
OPENAI_API_KEY=your_api_key_1,your_api_key_2

# OpenAI API Base URL (可选，默认为官方OpenAI API)
# 对于官方OpenAI API，可以留空或设置为: https://api.openai.com/v1
# 对于兼容的提供商，设置为他们的API端点
OPENAI_BASE_URL=https://api.openai.com/v1

# Default model name (可选，默认为 gpt-4-turbo)
DEFAULT_MODEL=gpt-4-turbo

# Timeout for API calls in milliseconds (可选，默认为 30000)
TIMEOUT_MS=30000